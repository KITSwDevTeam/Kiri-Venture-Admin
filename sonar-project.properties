# the project key must be unique 
sonar.projectKey=kiri:venture:admin
# this is the name and version displayed in the sonarqube UI
sonar.projectName=Kiriventure-Admin
sonar.projectVersion=0.0.1

# SQ standard properties
# this property is optional if sonar.module is set
sonar.sources=app, resources

# Encoding of the source code
sonar.sourceEncoding=UTF-8

# Properties specific to language plugins:
sonar.javascript.lcov.reportPath=resources/

sonar.exclusions=app/**